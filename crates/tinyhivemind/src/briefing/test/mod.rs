//! Unit tests for ephemeral team initialization, grouped by behavior area:
//! wire shapes, snapshot construction, system-text rendering, and session
//! context. Shared fixtures live in [`support`].

mod support;

mod context;
mod rendering;
mod snapshots;
mod wire;
